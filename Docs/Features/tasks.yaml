Components:
  Instance:
    Complexity: 80% 
  Adapter:
    Complexity: 20%

Tasks:
  Instance:
  Gossip Instance:
  Adapter:
    Setup:
      - Setting.py Configuration
      - Structure
      - Should notify when there is about to be trouble at an instance
    Methods:
      - getInstances
      - uploadFile
      - signFile
      - ipv4Access
      - deleteSignature
      - deleteIpv4Access
      - deleteFile 
      - setHighAvailability
      - setHighAvailabilityInMemory
    Dependencies:
      - Instance
      - Gossip Instance

    
  Arrangement:
    - Container Compatability:
      Dependencies:
        - Instance
    - Enviroment Variable Mapping:
      Dependencies:
        - Instance

Backlog:

After Research:
  - Reset file usage after an hour
  - Query parser exception handle